services:
  jupyter:
    build: .
    container_name: e-cert-jupyter
    ports:
      - "8888:8888"
    volumes:
      - .:/app
    environment:
      PYTHONUNBUFFERED: "1"
    command: >
      jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root